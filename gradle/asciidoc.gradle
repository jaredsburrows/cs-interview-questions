asciidoctor {
    description = project.description

    attributes = ["source-highlighter": "coderay",
                  "linkcss"           : true,
                  "title"             : project.name]
}
