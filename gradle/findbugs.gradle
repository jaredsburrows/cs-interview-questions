apply plugin: "findbugs"

tasks.withType(FindBugs) {
    reports {
        xml {
            enabled = false
        }

        html {
            enabled = true
        }
    }

    effort = "max"
    reportLevel = "high"
}
